# 7/9 周四 · 今日计划 v2（基于真实 Jimmy Song 章节）

> **修正**：先前误判剩的章节。实际剩 2 章 = **⑧ MCP 集成** + **⑨ 高级技巧**。
> **你已读**：概述 / 核心技术 / 输出配置 / 最佳实践 / Jinja2 模板 / 面向工程环境 / PromptOps 工作流。
> **今日目标**：读完剩 2 章 ⑦ 100% 收尾；把已读的 Jinja2 + PromptOps 落到 week1 Demo（这两章你读过但代码没吃到）。

---

## 上午 · 读剩 2 章（09:30-11:30，2h）

### 09:30-10:20　⑧ MCP 集成（50min，重点，week3 前置）

**主资料**：⭐ [提示词工程核心技术 - Jimmy Song · MCP 集成章](https://jimmysong.io/zh/book/ai-handbook/prompt/techniques/)

**读的时候回答自己 3 个问题**：
1. Jimmy Song 视角：MCP 和 Prompt 的**接口点**在哪？Prompt 怎么"知道"有哪些 MCP 工具可用？
2. MCP Server 暴露的工具，`description` 字段和 System Prompt 的"能力边界"是什么关系？谁写谁？
3. Jimmy Song 讲的 MCP 用法 vs OpenAI Function Calling（week1 Day2 已实现）有什么本质差异？

**读完带回**：
- 一段话概括：从 Prompt 工程视角看，MCP 到底解决了什么 Function Calling 没解决的问题？
- 一个疑问：你觉得 week3 用 MCP 重构 week1/2 的工具，会比现在的 `TOOLS` 列表好在哪？（可以写不确定）

> 这一章的价值不在"能落地进 week1 Demo"（week3 才做 MCP），而在**把 week3 的 MCP 学习目标从"陌生技术"变成"Prompt 工程视角下自然的延伸"**。

### 10:20-10:30　休息

### 10:30-11:20　⑨ 高级技巧（50min）

**主资料**：⭐ [提示词工程核心技术 - Jimmy Song · 高级技巧章](https://jimmysong.io/zh/book/ai-handbook/prompt/techniques/)

**读的时候留意 3 类内容**（书里通常会覆盖）：
1. **CoT / Self-Consistency / ToT** 这类推理增强技巧
2. **Prompt Injection 高级攻击** 和相应的防御升级（week1 只做了 XML 隔离，够不够）
3. 其它零碎但关键的技巧（如少样本示例排序、指令位置、否定 Prompt 效果差等）

**读完带回**：
- 3 条你觉得可以直接落到 week1 Demo 的技巧（每条一句话说做什么）
- 1 条你觉得不适合当前场景但值得记住的（写进面试速记卡）

### 11:20-11:30　收尾笔记（10min）

用一张纸/一段文本回答自己：
- ⑦ 提示词工程整本读完，我现在最有把握的是 ______
- 还没把握的是 ______
- 落到 week1 Demo 代码的抓手是 ______

---

## 下午 · 补代码（14:00-17:30，3.5h）

**下午的抓手**：不是"读完再补"，而是把**已读但代码没落地的 3 项**补上——这三项正好是 ⑦ 后半本你已读的核心：

| 已读章节 | week1 Demo 是否落地 | 补什么 |
|---------|---------------------|--------|
| Jinja2 提示词模板（第 5） | ❌ 现在是 f-string 拼接 | 抽 Jinja2 模板 |
| 面向工程环境的设计（第 6） | ❌ Prompt 散在字符串里 | 抽 prompts 包 |
| PromptOps 工作流（第 7） | ❌ 无版本号、无灰度 | 加版本号 + 最小 A/B |
| CoT/思维链（若第 2 章"核心技术"或第 9 章"高级技巧"讲了） | ❌ 未落地 | 加 `<thinking>` 引导 + 演示 4 |

### 14:00-15:30　抽 Jinja2 模板 + prompts 包（1.5h）

**改造 `scripts/week1/day3_system_prompt.py`**：

1. `pip install jinja2`
2. 新建目录：
   ```
   scripts/week1/prompts/
     __init__.py                 # 版本号常量、导出 render 函数
     system.jinja                # 系统级模板（有 {{ today }} 变量）
     scenario_v1.jinja           # 场景级 v1（当前版本）
   ```
3. `day3_system_prompt.py` 里 `build_system_level_prompt` / `SCENARIO_PROMPT` 改成 `render_template("system.jinja", today=date.today())`
4. 跑通验证：`python day3_system_prompt.py`，输出应和之前一致

### 15:30-16:30　CoT + 版本号 + 最小 A/B（1h）

1. 新增 `prompts/scenario_v2_cot.jinja`：在 v1 基础上加 `<thinking>` 引导 + Few-shot 示例含推理过程
2. `prompts/__init__.py` 加 `PROMPT_VERSIONS = {"v1": "scenario_v1.jinja", "v2_cot": "scenario_v2_cot.jinja"}`
3. 加 `get_scenario_prompt(user_id, ab_ratio=0.5)`：按 user_id hash 分流
4. `day3_system_prompt.py` 演示末尾加 demo 4：**多约束排优先级**（真实需要 CoT 的场景），跑通看 `<thinking>` 有没有出现

### 16:30-17:30　文档 + 提交（1h）

1. 写 `docs/week1/day3_extension.md`：Jinja2 + PromptOps 落地记录 + Jimmy Song ⑧⑨ 章读后感（3-5 句）
2. 更新 `docs/week1/week1_串讲总结.md` 主线，把 Jinja2/PromptOps 加到"day3 后续叠加"
3. 精确 `git add` + commit（子仓库内）：
   - `feat(agent-training): week1 Day3 补 Jinja2 模板 + Prompt 版本管理 + CoT 引导`

---

## 兜底

- 若上午读得快，11:30 前读完，下午提前开工
- 若下午来不及，**优先做 Jinja2 + prompts 包**（最有价值，是 PromptOps 的基础），A/B 分流可挪到周末
- 若 Jinja2 安装出问题，回退方案：Python 内建 `string.Template`，功能弱但零依赖

---

## 明天 7/10 周五预告

- ⑦ 已 100% 收尾（今天完成）
- Day 4 原理速览（Token / Context / Temperature）
- week2 预习（All-in-RAG + Milvus 环境）

> 今天读的 ⑧ MCP 集成，是 week3（7/20-24）的直接前置。读完后 week3 学 MCP 时不会陌生。
