# Session: 评估体系语义层修正 + 分层基线 + 坑 20-22 修复（08-09 晚 ~ 08-10 凌晨）

> 日期：2026-08-09 ~ 08-10
> 话题：语义层自适应修正 + 分层基线诊断 + 真实评估链路问题（预算/压缩/死循环）定位与修复
> 状态：坑 22 已修复，全量 139 passed，真实评估 4/10 完整跑通报告生成

---

## 本次完成

### 1. 语义层自适应修正（9.7 方案，用户确认三项决策）

- **judge.py**：无 context 时 faithfulness 不计权（`faithfulness_evaluated=False`）+ relevancy 走真实 judge（`build_relevancy_messages`），返回 `has_context`
- **runner.py**：语义层聚合自适应——有 context 用 `(faith+rel)/2`，无 context 只用 `rel`
- **print_summary**：分层基线诊断（工具≥0.7/轨迹≥0.6/语义≥0.5），综合 7/10 仍是门禁，语义层仅统计有 context case
- **report.py**：语义列统一 `_semantic_score()` 自适应 + 修复 avg_sem=None 崩溃

### 2. 真实回归基线对比

| 指标 | 改造前 | 改造后 |
|------|--------|--------|
| 综合通过 | 4/10 | **7/10**（中途验证）|
| 语义层 | 0.16 | 0.33（仅统计有 context case）|
| 轨迹层 | 0.56 | **0.89**（坑22修复后）|

### 3. 踩坑记录（全部落盘方案文档 9.8 节）

| 坑 | 根因 | 修复 | 提交 |
|----|------|------|------|
| 坑 20 | eval_001/005/007 生成 `<tool_calls>` 残留非文本 | 待修 generate_answer | — |
| 坑 21 | 压缩器 old 为空跳过截断 | old 为空仍超预算时截断 | f92a85a |
| 坑 22 | 纯文本轮 iteration 不递增 → needs_more 永真 → 死循环 | 方案1 纯文本轮递增 + 方案3 evaluate 循环检测 | dafe60e |

### 4. 坑 22 完整定位过程（systematic-debugging + codegraph）

4 轮排查（CLI 管道→预算→压缩器→iteration 卡 2），关键：debug stream 打节点序列发现 `iteration: 2` 卡死。完整时间线已落盘方案文档。

## 关键决策点（用户已定）

- ✅ 保留语义层权重 0.2
- ✅ 分层阈值：工具 0.7 / 轨迹 0.6 / 语义 0.5
- ✅ 分层基线作诊断报告（与 7/10 综合并存，不判 fail）
- ✅ 坑 22 修复方案 1+3（纯文本轮递增 + 循环检测）

## 产出物

- `demo/eval/` 语义层自适应 + 分层基线 + 报告修复
- `demo/graph/` 坑 21/22 修复
- `docs/week6/demo-评估改造方案-2026-08-09.md` 9.7/9.8 节（方案 + 坑 17-22 + 时间线）
- `docs/superpowers/plans/2026-08-09-评估体系语义层修正与分层基线.md` 实现计划
- `demo/run_eval_report.py` 独立评估脚本（绕开 CLI 管道）

## 待办（下次继续）

- [ ] 坑 20：generate_answer 检测 `<tool_calls>` 残留重试（修后回归基线可能再升）
- [ ] 轨迹层阈值 D5 细化（0.89 已达标，看是否过度惩罚合理多步）
- [ ] L1 缓存评估前清理（D6）
- [ ] 真实评估预算上限独立配置（LLM_BUDGET_LIMIT 当前需手动 20）

## 关联 session

- session-2026-08-09-demo三层评估体系实现+全量测试+回测.md（评估体系实现）
- session-2026-08-09-demo制造业智能体缺失评估报告.md（行业缺失评估）
