# <功能名> 实现计划

**关联规格：** `../02-specs/<spec>.md`
**门禁1 自查：** 规格每条验收标准有对应任务 □ / 末尾有 E2E 任务 □ / 9 角色评估 PASS □

## Phase 1 — <层名>

### 任务 T1.1：<一句话>

**文件：** 创建/修改 `<精确路径>`

- [ ] 步骤 1：<动作>
- [ ] 步骤 2：**自动验收命令**：`<命令>` → 预期 `<输出>`

（每任务 ≤3 变更点；Phase 结束跑构建 + 相关测试 + 回核 spec 该 Phase 涉及的全部验收标准）

## 最终任务：端到端验收（必须最后）

- [ ] 逐条核对 spec §5：`<逐条命令>`
- [ ] **自动验收命令（默认）**：`sh <docs仓>/08-test/smoke-test.sh` → 预期 `SMOKE PASS`（一处定义一处引用）
- [ ] 输出报告：spec N 条 → M 条 PASS，0 遗漏
